name: Build and Push Football App

on:
  push:
    branches:
      - main
      - feature/multigroup
  workflow_dispatch:

env:
  IMAGE_NAME: football-app

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Harbor
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.HARBOR_REGISTRY }}
          username: ${{ secrets.HARBOR_USERNAME }}
          password: ${{ secrets.HARBOR_PASSWORD }}

      - name: Build Docker image
        run: |
          docker build \
            -t ${{ secrets.HARBOR_REGISTRY }}/football/football-app:${{ github.sha }} \
            -t ${{ secrets.HARBOR_REGISTRY }}/football/football-app:latest .

      - name: Push Docker image
        run: |
          docker push ${{ secrets.HARBOR_REGISTRY }}/football/football-app:${{ github.sha }}
          docker push ${{ secrets.HARBOR_REGISTRY }}/football/football-app:latest
